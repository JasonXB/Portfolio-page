export const styles = {
  outerBox: (mui) => ({
    width: "calc(100vw - (100vw - 100%))",
    // background: mui.palette.bg.main, //!!!
  }),
  innerBox: (mui) => ({
    width: "100%",
    maxWidth: "1232px", // 1200 after px = 2
    margin: "auto",
    // background: mui.palette.bg.main, //!!!
  }),
};
